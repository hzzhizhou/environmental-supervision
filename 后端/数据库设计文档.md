# 东软环保公众监督系统 - 数据库设计文档

---

## 文档信息

| 属性 | 值 |
|------|-----|
| **文档版本** | v1.0 |
| **创建日期** | 2026-06-05 |
| **数据库名称** | `epm_db` |
| **字符集** | utf8mb4 |
| **排序规则** | utf8mb4_unicode_ci |
| **存储引擎** | InnoDB |

---

## 目录

1. [数据库概述](#1-数据库概述)
2. [表结构设计](#2-表结构设计)
   - 2.1 角色表 (roles)
   - 2.2 网格表 (grids)
   - 2.3 用户表 (users)
   - 2.4 短信验证码表 (sms_logs)
   - 2.5 公众AQI上报表 (aqi_reports)
   - 2.6 指派任务表 (task_assignments)
   - 2.7 实测数据表 (detect_data)
   - 2.8 附件表 (attachments)
   - 2.9 审核记录表 (audit_logs)
   - 2.10 统计汇总表 (statistics)
3. [表关系图](#3-表关系图)
4. [索引设计](#4-索引设计)
5. [数据字典](#5-数据字典)
6. [业务流程](#6-业务流程)

---

## 1. 数据库概述

### 1.1 系统简介

东软环保公众监督系统是一个面向公众的空气质量监督平台，主要功能包括：
- 公众监督员上报空气质量数据
- 系统自动匹配网格区域
- 管理员指派任务给网格员
- 网格员现场检测并提交实测数据
- 管理员审核确认数据
- 决策者查看统计分析数据

### 1.2 数据库设计原则

| 原则 | 说明 |
|------|------|
| 规范化设计 | 遵循第三范式(3NF)，减少数据冗余 |
| 主键自增 | 使用 INT AUTO_INCREMENT 作为主键 |
| 外键约束 | 建立外键关联，保证数据完整性 |
| 索引优化 | 合理创建索引，提升查询性能 |
| 字符集统一 | 使用 utf8mb4 支持中文和emoji |

---

## 2. 表结构设计

### 2.1 角色表 (roles)

**功能说明**：存储系统支持的角色类型，用于权限控制

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| role_id | INT | PRIMARY KEY AUTO_INCREMENT | 角色唯一标识 |
| role_name | VARCHAR(20) | NOT NULL | 角色显示名称，如"系统管理员" |
| role_code | VARCHAR(20) | NOT NULL UNIQUE | 角色代码：ADMIN, WORKER, PUBLIC, DECIDER |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 记录创建时间 |

**角色代码说明**：

| 角色代码 | 角色名称 | 说明 |
|---------|---------|------|
| ADMIN | 系统管理员 | 管理用户、指派任务、审核数据 |
| WORKER | 网格员 | 接收任务、现场检测、提交数据 |
| PUBLIC | 公众监督员 | 上报空气质量、查看记录 |
| DECIDER | 决策者 | 查看统计数据、大屏展示 |

**建表SQL**：
```sql
CREATE TABLE `roles` (
  `role_id` INT NOT NULL AUTO_INCREMENT COMMENT '角色唯一标识，主键',
  `role_name` VARCHAR(20) NOT NULL COMMENT '角色名称，如"系统管理员"、"网格员"',
  `role_code` VARCHAR(20) NOT NULL COMMENT '角色代码：ADMIN（系统管理员）、WORKER（网格员）、PUBLIC（公众监督员）、DECIDER（决策者）',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`role_id`),
  UNIQUE KEY `uk_role_code` (`role_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表，定义系统用户角色';
```

---

### 2.2 网格表 (grids)

**功能说明**：存储环保监督的网格区域信息，每个网格对应一个地理范围

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| grid_id | INT | PRIMARY KEY AUTO_INCREMENT | 网格唯一标识 |
| grid_code | VARCHAR(30) | NOT NULL UNIQUE | 网格编码，如 GRID-001 |
| grid_name | VARCHAR(50) | NOT NULL | 网格名称，如"和平区网格1" |
| province | VARCHAR(30) | NOT NULL | 省份名称 |
| city | VARCHAR(30) | NOT NULL | 城市名称 |
| district | VARCHAR(30) | DEFAULT NULL | 区/县名称 |
| status | TINYINT | DEFAULT 1 | 状态：1启用，0禁用 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**建表SQL**：
```sql
CREATE TABLE `grids` (
  `grid_id` INT NOT NULL AUTO_INCREMENT COMMENT '网格唯一标识，主键',
  `grid_code` VARCHAR(30) NOT NULL COMMENT '网格编码，例如 GRID-001，唯一标识一个网格',
  `grid_name` VARCHAR(50) NOT NULL COMMENT '网格名称，例如"和平区网格1"',
  `province` VARCHAR(30) NOT NULL COMMENT '省份名称，例如"辽宁省"',
  `city` VARCHAR(30) NOT NULL COMMENT '城市名称，例如"沈阳市"',
  `district` VARCHAR(30) DEFAULT NULL COMMENT '区/县名称，例如"和平区"',
  `status` TINYINT DEFAULT 1 COMMENT '网格状态：1-启用（公众可选择），0-禁用（不显示）',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`grid_id`),
  UNIQUE KEY `uk_grid_code` (`grid_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='网格表，存储环保监督的地理网格区域';
```

---

### 2.3 用户表 (users)

**功能说明**：存储所有系统用户，包括公众监督员、网格员、系统管理员、决策者

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| user_id | INT | PRIMARY KEY AUTO_INCREMENT | 用户唯一标识 |
| username | VARCHAR(50) | NOT NULL UNIQUE | 登录账号，唯一 |
| password | VARCHAR(255) | NOT NULL | 加密密码（BCrypt） |
| real_name | VARCHAR(30) | DEFAULT NULL | 真实姓名 |
| phone | VARCHAR(11) | NOT NULL UNIQUE | 手机号，用于登录和通知 |
| role_id | INT | NOT NULL | 角色ID，关联 roles.role_id |
| grid_id | INT | DEFAULT NULL | 网格ID，关联 grids.grid_id（仅网格员） |
| avatar_url | VARCHAR(255) | DEFAULT NULL | 头像URL |
| allow_cross_region | TINYINT | DEFAULT 0 | 是否允许跨区域接受任务 |
| status | TINYINT | DEFAULT 1 | 账号状态：1启用，0禁用 |
| last_login_at | TIMESTAMP | NULL | 最后登录时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 注册时间 |

**用户类型说明**：

| 角色 | role_id | grid_id | allow_cross_region |
|------|---------|---------|-------------------|
| 系统管理员 | 1 | NULL | 无效 |
| 网格员 | 2 | 必填 | 有效 |
| 公众监督员 | 3 | NULL | 无效 |
| 决策者 | 4 | NULL | 无效 |

**建表SQL**：
```sql
CREATE TABLE `users` (
  `user_id` INT NOT NULL AUTO_INCREMENT COMMENT '用户唯一标识，主键',
  `username` VARCHAR(50) NOT NULL COMMENT '登录账号，全局唯一',
  `password` VARCHAR(255) NOT NULL COMMENT '加密后的密码',
  `real_name` VARCHAR(30) DEFAULT NULL COMMENT '真实姓名（可选）',
  `phone` VARCHAR(11) NOT NULL COMMENT '手机号码，全局唯一，用于登录和接收短信',
  `role_id` INT NOT NULL COMMENT '角色ID，关联 roles 表，决定用户权限',
  `grid_id` INT DEFAULT NULL COMMENT '网格ID，关联 grids 表，仅网格员角色需要填写，表示管辖区域',
  `avatar_url` VARCHAR(255) DEFAULT NULL COMMENT '头像图片地址',
  `allow_cross_region` TINYINT DEFAULT 0 COMMENT '是否允许接受异地任务：0-不允许，1-允许。仅网格员使用',
  `status` TINYINT DEFAULT 1 COMMENT '账号状态：1-正常，0-禁用（禁用后无法登录）',
  `last_login_at` TIMESTAMP NULL COMMENT '最后登录时间，用于统计用户活跃度',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '账号注册时间',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_phone` (`phone`),
  KEY `idx_role_id` (`role_id`),
  KEY `idx_grid_id` (`grid_id`),
  FOREIGN KEY (`role_id`) REFERENCES `roles` (`role_id`) ON DELETE RESTRICT ON UPDATE CASCADE,
  FOREIGN KEY (`grid_id`) REFERENCES `grids` (`grid_id`) ON DELETE SET NULL ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表，存储所有角色用户（公众、网格员、管理员、决策者）';
```

---

### 2.4 短信验证码表 (sms_logs)

**功能说明**：存储发送的短信验证码，用于注册和登录验证

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| sms_id | INT | PRIMARY KEY AUTO_INCREMENT | 记录唯一标识 |
| phone | VARCHAR(11) | NOT NULL | 接收验证码的手机号 |
| code | VARCHAR(6) | NOT NULL | 6位数字验证码 |
| type | VARCHAR(20) | NOT NULL | 验证码用途：register/login/reset |
| is_used | TINYINT | DEFAULT 0 | 是否已使用：0未使用，1已使用 |
| expired_at | TIMESTAMP | NOT NULL | 过期时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 发送时间 |

**验证码类型说明**：

| 类型 | 说明 |
|------|------|
| register | 用户注册 |
| login | 用户登录 |
| reset | 重置密码 |

**建表SQL**：
```sql
CREATE TABLE `sms_logs` (
  `sms_id` INT NOT NULL AUTO_INCREMENT COMMENT '短信记录唯一标识',
  `phone` VARCHAR(11) NOT NULL COMMENT '接收验证码的手机号',
  `code` VARCHAR(6) NOT NULL COMMENT '6位数字验证码',
  `type` VARCHAR(20) NOT NULL COMMENT '验证码类型：register（注册）、login（登录）、reset（重置密码）',
  `is_used` TINYINT DEFAULT 0 COMMENT '是否已使用：0-未使用，1-已使用（防止重复验证）',
  `expired_at` TIMESTAMP NOT NULL COMMENT '验证码过期时间，通常为发送时间+5分钟',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '验证码发送时间',
  PRIMARY KEY (`sms_id`),
  KEY `idx_phone_code` (`phone`, `code`),
  KEY `idx_expired` (`expired_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='短信验证码记录表，用于用户注册、登录等场景的验证码校验';
```

---

### 2.5 公众AQI上报表 (aqi_reports)

**功能说明**：存储公众监督员提交的空气质量反馈信息

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| report_id | INT | PRIMARY KEY AUTO_INCREMENT | 上报记录唯一标识 |
| report_code | VARCHAR(30) | NOT NULL UNIQUE | 上报编号：AQI-YYYYMMDD-XXX |
| user_id | INT | NOT NULL | 上报人ID，关联 users.user_id |
| aqi_value | INT | NOT NULL | AQI数值（0-500） |
| aqi_level | VARCHAR(20) | NOT NULL | AQI等级：优、良、轻度污染、中度污染、重度污染 |
| province | VARCHAR(30) | NOT NULL | 省份名称 |
| city | VARCHAR(30) | NOT NULL | 城市名称 |
| address | VARCHAR(200) | DEFAULT NULL | 详细地址 |
| grid_id | INT | DEFAULT NULL | 系统自动匹配的网格ID |
| status | VARCHAR(20) | DEFAULT 'pending' | 状态：pending/assigned/detected/confirmed |
| remark | VARCHAR(500) | DEFAULT NULL | 用户填写的污染描述 |
| pm25 | DECIMAL(8,2) | DEFAULT NULL | PM2.5浓度（μg/m³） |
| pm10 | DECIMAL(8,2) | DEFAULT NULL | PM10浓度（μg/m³） |
| o3 | DECIMAL(8,2) | DEFAULT NULL | 臭氧浓度（μg/m³） |
| so2 | DECIMAL(8,2) | DEFAULT NULL | 二氧化硫浓度（μg/m³） |
| no2 | DECIMAL(8,2) | DEFAULT NULL | 二氧化氮浓度（μg/m³） |
| co | DECIMAL(8,2) | DEFAULT NULL | 一氧化碳浓度（mg/m³） |
| reported_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 上报时间 |
| confirmed_at | TIMESTAMP | NULL | 管理员确认时间 |

**状态流转**：

```
pending（待指派） → assigned（已指派） → detected（已检测） → confirmed（已确认）
```

**建表SQL**：
```sql
CREATE TABLE `aqi_reports` (
  `report_id` INT NOT NULL AUTO_INCREMENT COMMENT '上报记录ID，主键',
  `report_code` VARCHAR(30) NOT NULL COMMENT '上报编号，格式 AQI-年月日-序号，例如 AQI-20260603-001',
  `user_id` INT NOT NULL COMMENT '公众监督员ID，关联 users.user_id',
  `aqi_value` INT NOT NULL COMMENT 'AQI数值，范围 0-500',
  `aqi_level` VARCHAR(20) NOT NULL COMMENT 'AQI等级：优、良、轻度污染、中度污染、重度污染',
  `province` VARCHAR(30) NOT NULL COMMENT '省份名称，例如"辽宁省"',
  `city` VARCHAR(30) NOT NULL COMMENT '城市名称，例如"沈阳市"',
  `address` VARCHAR(200) DEFAULT NULL COMMENT '详细地址（街道、门牌号等）',
  `grid_id` INT DEFAULT NULL COMMENT '系统自动匹配的网格ID，关联 grids.grid_id',
  `status` VARCHAR(20) DEFAULT 'pending' COMMENT '处理状态：pending（待指派）、assigned（已指派）、detected（网格员已提交实测）、confirmed（管理员已确认）',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '用户填写的污染描述或备注',
  `pm25` DECIMAL(8,2) DEFAULT NULL COMMENT 'PM2.5浓度（μg/m³）',
  `pm10` DECIMAL(8,2) DEFAULT NULL COMMENT 'PM10浓度（μg/m³）',
  `o3` DECIMAL(8,2) DEFAULT NULL COMMENT '臭氧浓度（μg/m³）',
  `so2` DECIMAL(8,2) DEFAULT NULL COMMENT '二氧化硫浓度（μg/m³）',
  `no2` DECIMAL(8,2) DEFAULT NULL COMMENT '二氧化氮浓度（μg/m³）',
  `co` DECIMAL(8,2) DEFAULT NULL COMMENT '一氧化碳浓度（mg/m³）',
  `reported_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '上报时间',
  `confirmed_at` TIMESTAMP NULL COMMENT '最终确认时间',
  PRIMARY KEY (`report_id`),
  UNIQUE KEY `uk_report_code` (`report_code`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status_grid` (`status`, `grid_id`),
  KEY `idx_reported_at` (`reported_at`),
  FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE RESTRICT ON UPDATE CASCADE,
  FOREIGN KEY (`grid_id`) REFERENCES `grids` (`grid_id`) ON DELETE SET NULL ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='公众AQI上报表，记录公众监督员提交的空气质量反馈信息';
```

---

### 2.6 指派任务表 (task_assignments)

**功能说明**：记录管理员将公众上报指派给网格员的每一次任务

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| task_id | INT | PRIMARY KEY AUTO_INCREMENT | 任务唯一标识 |
| report_id | INT | NOT NULL UNIQUE | 关联的公众上报ID |
| admin_id | INT | NOT NULL | 执行指派的管理员ID |
| worker_id | INT | NOT NULL | 被指派的网格员ID |
| assign_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 指派时间 |
| deadline | DATETIME | DEFAULT NULL | 截止时间（可选） |
| status | VARCHAR(20) | DEFAULT 'assigned' | 状态：assigned/completed/rejected |
| reject_reason | VARCHAR(255) | DEFAULT NULL | 驳回原因 |

**建表SQL**：
```sql
CREATE TABLE `task_assignments` (
  `task_id` INT NOT NULL AUTO_INCREMENT COMMENT '任务ID，主键',
  `report_id` INT NOT NULL COMMENT '关联的公众上报ID',
  `admin_id` INT NOT NULL COMMENT '指派人（管理员）ID',
  `worker_id` INT NOT NULL COMMENT '被指派的网格员ID',
  `assign_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '指派时间',
  `deadline` DATETIME DEFAULT NULL COMMENT '要求完成的截止时间（可选）',
  `status` VARCHAR(20) DEFAULT 'assigned' COMMENT '任务状态：assigned（已指派）、completed（网格员已完成实测）、rejected（管理员驳回）',
  `reject_reason` VARCHAR(255) DEFAULT NULL COMMENT '若任务被驳回，记录驳回原因',
  PRIMARY KEY (`task_id`),
  UNIQUE KEY `uk_report_id` (`report_id`),
  KEY `idx_worker_id` (`worker_id`),
  FOREIGN KEY (`report_id`) REFERENCES `aqi_reports` (`report_id`) ON DELETE RESTRICT ON UPDATE CASCADE,
  FOREIGN KEY (`admin_id`) REFERENCES `users` (`user_id`) ON DELETE RESTRICT ON UPDATE CASCADE,
  FOREIGN KEY (`worker_id`) REFERENCES `users` (`user_id`) ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='指派任务表，记录管理员将公众上报指派给网格员的任务信息';
```

---

### 2.7 实测数据表 (detect_data)

**功能说明**：存储网格员现场检测后提交的实测空气质量数据

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| detect_id | INT | PRIMARY KEY AUTO_INCREMENT | 实测记录唯一标识 |
| task_id | INT | NOT NULL UNIQUE | 关联的指派任务ID |
| worker_id | INT | NOT NULL | 执行检测的网格员ID |
| detect_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 检测时间 |
| aqi_value | INT | NOT NULL | 实测AQI数值 |
| aqi_level | VARCHAR(20) | NOT NULL | 实测AQI等级 |
| pm25 | DECIMAL(8,2) | DEFAULT NULL | PM2.5实测值 |
| pm10 | DECIMAL(8,2) | DEFAULT NULL | PM10实测值 |
| o3 | DECIMAL(8,2) | DEFAULT NULL | 臭氧实测值 |
| so2 | DECIMAL(8,2) | DEFAULT NULL | 二氧化硫实测值 |
| no2 | DECIMAL(8,2) | DEFAULT NULL | 二氧化氮实测值 |
| co | DECIMAL(8,2) | DEFAULT NULL | 一氧化碳实测值 |
| photos | VARCHAR(1000) | DEFAULT NULL | 现场照片URL列表（JSON数组） |
| confirm_status | TINYINT | DEFAULT 0 | 确认状态：0待确认，1已确认，2驳回 |
| confirm_note | VARCHAR(255) | DEFAULT NULL | 驳回备注 |
| confirmed_at | TIMESTAMP | NULL | 确认时间 |

**建表SQL**：
```sql
CREATE TABLE `detect_data` (
  `detect_id` INT NOT NULL AUTO_INCREMENT COMMENT '实测数据ID，主键',
  `task_id` INT NOT NULL COMMENT '关联的指派任务ID',
  `worker_id` INT NOT NULL COMMENT '执行检测的网格员ID',
  `detect_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '检测提交时间',
  `aqi_value` INT NOT NULL COMMENT '实测AQI数值',
  `aqi_level` VARCHAR(20) NOT NULL COMMENT '实测AQI等级',
  `pm25` DECIMAL(8,2) DEFAULT NULL COMMENT 'PM2.5实测值（μg/m³）',
  `pm10` DECIMAL(8,2) DEFAULT NULL COMMENT 'PM10实测值（μg/m³）',
  `o3` DECIMAL(8,2) DEFAULT NULL COMMENT '臭氧实测值（μg/m³）',
  `so2` DECIMAL(8,2) DEFAULT NULL COMMENT '二氧化硫实测值（μg/m³）',
  `no2` DECIMAL(8,2) DEFAULT NULL COMMENT '二氧化氮实测值（μg/m³）',
  `co` DECIMAL(8,2) DEFAULT NULL COMMENT '一氧化碳实测值（mg/m³）',
  `photos` VARCHAR(1000) DEFAULT NULL COMMENT '现场照片URL列表，JSON数组格式',
  `confirm_status` TINYINT DEFAULT 0 COMMENT '管理员确认状态：0-待确认，1-已确认，2-驳回',
  `confirm_note` VARCHAR(255) DEFAULT NULL COMMENT '管理员驳回时的备注说明',
  `confirmed_at` TIMESTAMP NULL COMMENT '管理员确认（或驳回）的时间',
  PRIMARY KEY (`detect_id`),
  UNIQUE KEY `uk_task_id` (`task_id`),
  KEY `idx_worker_id` (`worker_id`),
  KEY `idx_confirm_status` (`confirm_status`),
  FOREIGN KEY (`task_id`) REFERENCES `task_assignments` (`task_id`) ON DELETE RESTRICT ON UPDATE CASCADE,
  FOREIGN KEY (`worker_id`) REFERENCES `users` (`user_id`) ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='实测数据表，存储网格员现场检测提交的AQI实测数据';
```

---

### 2.8 附件表 (attachments)

**功能说明**：统一存储公众上报和网格员检测时上传的图片附件

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| attach_id | INT | PRIMARY KEY AUTO_INCREMENT | 附件唯一标识 |
| report_id | INT | DEFAULT NULL | 关联的公众上报ID |
| detect_id | INT | DEFAULT NULL | 关联的实测数据ID |
| file_url | VARCHAR(500) | NOT NULL | 附件存储的URL或路径 |
| file_type | VARCHAR(20) | DEFAULT 'image' | 文件类型 |
| upload_type | VARCHAR(20) | NOT NULL | 上传来源：report/detect |
| uploaded_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 上传时间 |

**建表SQL**：
```sql
CREATE TABLE `attachments` (
  `attach_id` INT NOT NULL AUTO_INCREMENT COMMENT '附件ID，主键',
  `report_id` INT DEFAULT NULL COMMENT '关联的公众上报ID',
  `detect_id` INT DEFAULT NULL COMMENT '关联的实测数据ID',
  `file_url` VARCHAR(500) NOT NULL COMMENT '附件存储的URL或服务器路径',
  `file_type` VARCHAR(20) DEFAULT 'image' COMMENT '文件类型，如 image',
  `upload_type` VARCHAR(20) NOT NULL COMMENT '上传类型：report（公众上报）、detect（网格员检测）',
  `uploaded_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '上传时间',
  PRIMARY KEY (`attach_id`),
  KEY `idx_report_id` (`report_id`),
  KEY `idx_detect_id` (`detect_id`),
  FOREIGN KEY (`report_id`) REFERENCES `aqi_reports` (`report_id`) ON DELETE CASCADE ON UPDATE CASCADE,
  FOREIGN KEY (`detect_id`) REFERENCES `detect_data` (`detect_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='附件表，存储公众上报和网格员检测时上传的图片文件';
```

---

### 2.9 审核记录表 (audit_logs)

**功能说明**：记录管理员对网格员提交的实测数据进行确认或驳回的操作日志

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| audit_id | INT | PRIMARY KEY AUTO_INCREMENT | 审核记录唯一标识 |
| detect_id | INT | NOT NULL | 关联的实测数据ID |
| auditor_id | INT | NOT NULL | 执行审核的管理员ID |
| action | VARCHAR(20) | NOT NULL | 审核动作：confirm/reject |
| result | TEXT | COMMENT | 审核备注 |
| audited_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 审核时间 |

**建表SQL**：
```sql
CREATE TABLE `audit_logs` (
  `audit_id` INT NOT NULL AUTO_INCREMENT COMMENT '审核记录ID，主键',
  `detect_id` INT NOT NULL COMMENT '实测数据ID',
  `auditor_id` INT NOT NULL COMMENT '审核人（管理员）ID',
  `action` VARCHAR(20) NOT NULL COMMENT '审核动作：confirm（确认通过）、reject（驳回）',
  `result` TEXT COMMENT '审核备注',
  `audited_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '审核时间',
  PRIMARY KEY (`audit_id`),
  KEY `idx_detect_id` (`detect_id`),
  FOREIGN KEY (`detect_id`) REFERENCES `detect_data` (`detect_id`) ON DELETE RESTRICT ON UPDATE CASCADE,
  FOREIGN KEY (`auditor_id`) REFERENCES `users` (`user_id`) ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='审核记录表，记录管理员对实测数据的确认或驳回操作';
```

---

### 2.10 统计汇总表 (statistics)

**功能说明**：存储按月、按省/市预聚合的统计数据，用于决策者大屏展示

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| stat_id | INT | PRIMARY KEY AUTO_INCREMENT | 统计记录唯一标识 |
| province | VARCHAR(30) | NOT NULL | 省份名称 |
| city | VARCHAR(30) | DEFAULT NULL | 城市名称（NULL表示全省汇总） |
| stat_month | VARCHAR(7) | NOT NULL | 统计月份：YYYY-MM |
| total_reports | INT | DEFAULT 0 | 该月该区域公众上报总数 |
| confirmed_count | INT | DEFAULT 0 | 被确认的实测数据条数 |
| rejected_count | INT | DEFAULT 0 | 被驳回的实测数据条数 |
| avg_aqi | DECIMAL(6,2) | DEFAULT NULL | AQI平均值 |
| max_aqi | INT | DEFAULT NULL | AQI最大值 |
| min_aqi | INT | DEFAULT NULL | AQI最小值 |
| excellent_days | INT | DEFAULT 0 | AQI等级为"优"的天数 |
| good_days | INT | DEFAULT 0 | AQI等级为"良"的天数 |
| light_pollution_days | INT | DEFAULT 0 | 轻度污染天数 |
| moderate_pollution_days | INT | DEFAULT 0 | 中度污染天数 |
| heavy_pollution_days | INT | DEFAULT 0 | 重度污染天数 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**建表SQL**：
```sql
CREATE TABLE `statistics` (
  `stat_id` INT NOT NULL AUTO_INCREMENT COMMENT '统计记录ID，主键',
  `province` VARCHAR(30) NOT NULL COMMENT '省份名称',
  `city` VARCHAR(30) DEFAULT NULL COMMENT '城市名称，NULL表示全省汇总',
  `stat_month` VARCHAR(7) NOT NULL COMMENT '统计月份，格式 YYYY-MM',
  `total_reports` INT DEFAULT 0 COMMENT '该月该区域公众上报总数',
  `confirmed_count` INT DEFAULT 0 COMMENT '被确认的实测数据条数',
  `rejected_count` INT DEFAULT 0 COMMENT '被驳回的实测数据条数',
  `avg_aqi` DECIMAL(6,2) DEFAULT NULL COMMENT 'AQI平均值',
  `max_aqi` INT DEFAULT NULL COMMENT 'AQI最大值',
  `min_aqi` INT DEFAULT NULL COMMENT 'AQI最小值',
  `excellent_days` INT DEFAULT 0 COMMENT 'AQI等级为"优"的天数',
  `good_days` INT DEFAULT 0 COMMENT 'AQI等级为"良"的天数',
  `light_pollution_days` INT DEFAULT 0 COMMENT '轻度污染天数',
  `moderate_pollution_days` INT DEFAULT 0 COMMENT '中度污染天数',
  `heavy_pollution_days` INT DEFAULT 0 COMMENT '重度污染天数',
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  PRIMARY KEY (`stat_id`),
  UNIQUE KEY `uk_city_month` (`province`, `city`, `stat_month`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='统计汇总表，按月预聚合AQI数据，供决策者大屏使用';
```

---

## 3. 表关系图

```
roles (角色)
   └── users (用户) ──────┐
                          │
grids (网格) ────────────┘
   │
   └── aqi_reports (公众上报) ──┬─ attachments (附件)
                                └─ task_assignments (指派任务) ── detect_data (实测数据) ──┬─ attachments
                                                                                           └─ audit_logs (审核记录)
statistics (统计汇总) 独立，由定时任务维护
sms_logs (验证码) 独立
```

### 关系矩阵

| 主表 | 从表 | 外键 | 级联操作 |
|------|------|------|---------|
| roles | users | role_id | RESTRICT/SET NULL |
| grids | users | grid_id | SET NULL |
| grids | aqi_reports | grid_id | SET NULL |
| users | aqi_reports | user_id | RESTRICT |
| users | task_assignments | admin_id | RESTRICT |
| users | task_assignments | worker_id | RESTRICT |
| users | audit_logs | auditor_id | RESTRICT |
| aqi_reports | task_assignments | report_id | RESTRICT |
| aqi_reports | attachments | report_id | CASCADE |
| task_assignments | detect_data | task_id | RESTRICT |
| detect_data | attachments | detect_id | CASCADE |
| detect_data | audit_logs | detect_id | RESTRICT |

---

## 4. 索引设计

### 4.1 主键索引

| 表名 | 主键字段 |
|------|---------|
| roles | role_id |
| grids | grid_id |
| users | user_id |
| sms_logs | sms_id |
| aqi_reports | report_id |
| task_assignments | task_id |
| detect_data | detect_id |
| attachments | attach_id |
| audit_logs | audit_id |
| statistics | stat_id |

### 4.2 唯一索引

| 表名 | 唯一键字段 |
|------|---------|
| roles | role_code |
| grids | grid_code |
| users | username, phone |
| aqi_reports | report_code |
| task_assignments | report_id |
| detect_data | task_id |
| statistics | province, city, stat_month |

### 4.3 普通索引

| 表名 | 索引字段 | 用途 |
|------|---------|------|
| users | role_id, grid_id | 按角色/网格查询用户 |
| sms_logs | phone, code, expired_at | 验证码验证 |
| aqi_reports | user_id, status, grid_id, reported_at | 上报记录查询 |
| task_assignments | worker_id | 网格员任务查询 |
| detect_data | worker_id, confirm_status | 实测数据查询 |
| attachments | report_id, detect_id | 附件查询 |
| audit_logs | detect_id | 审核记录查询 |

---

## 5. 数据字典

### 5.1 AQI等级

| AQI范围 | 等级 | 说明 |
|---------|------|------|
| 0-50 | 优 | 空气质量令人满意，基本无污染 |
| 51-100 | 良 | 可接受，某些污染物可能对少数敏感人群有影响 |
| 101-150 | 轻度污染 | 敏感人群应减少户外活动 |
| 151-200 | 中度污染 | 所有人应减少户外活动 |
| 201-300 | 重度污染 | 所有人应避免户外活动 |
| >300 | 严重污染 | 所有人应留在室内 |

### 5.2 用户状态

| 值 | 说明 |
|-----|------|
| 0 | 禁用 |
| 1 | 启用 |

### 5.3 网格状态

| 值 | 说明 |
|-----|------|
| 0 | 禁用 |
| 1 | 启用 |

### 5.4 上报状态

| 值 | 说明 |
|-----|------|
| pending | 待指派 |
| assigned | 已指派 |
| detected | 已检测 |
| confirmed | 已确认 |

### 5.5 任务状态

| 值 | 说明 |
|-----|------|
| assigned | 已指派 |
| completed | 已完成 |
| rejected | 已驳回 |

### 5.6 确认状态

| 值 | 说明 |
|-----|------|
| 0 | 待确认 |
| 1 | 已确认 |
| 2 | 已驳回 |

---

## 6. 业务流程

### 6.1 上报流程

```
公众监督员 → 提交上报(aqi_reports) → 系统匹配网格 → 生成上报编号
     ↓
管理员 → 查询待指派上报 → 创建指派任务(task_assignments) → 更新上报状态为assigned
     ↓
网格员 → 查询任务 → 提交实测数据(detect_data) → 更新任务状态为completed
     ↓
管理员 → 审核确认(audit_logs) → 更新确认状态 → 更新上报状态为confirmed
     ↓
定时任务 → 更新统计汇总(statistics)
```

### 6.2 用户注册流程

```
用户 → 获取验证码(sms_logs) → 验证验证码 → 创建用户(users)
```

### 6.3 登录流程

```
用户 → 获取验证码(sms_logs) → 验证验证码 → 更新last_login_at
```

---

**文档结束**